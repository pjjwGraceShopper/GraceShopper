import React, { useState, useEffect } from "react";

//----------------------------------------------------------------
const Cart_Item = ({ cart }) => {
  //-----------------------------------------------------------
console.log(`cart in CART_ITEM:`, cart[0].items)

//-------------------------------------------
  return(
<>
       <div>HI </div>
</>
  )
}
//----------------------------------------------------------------
export default Cart_Item;

// cart.map((e, i) => { return <div className="cart-item" key={i}> {e} </div>; })